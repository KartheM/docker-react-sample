// @flow
import React from "react";
import ReactDOM from "react-dom";
import GuardCircle from "./guard.js";

const rootElement = document.getElementById("root");
ReactDOM.render(<GuardCircle />, rootElement);
